name: "Personal Deploy"

on:
    push:
        branches: 
            - 'deploy'
    # Also allow manually running the workflow
    workflow_dispatch:

jobs:
    deploy:
        runs-on: ubuntu-latest

        defaults:
            run:
                working-directory: web

        steps:
            - name: Checkout code
              uses: actions/checkout@v4
              with:
                  submodules: recursive

            - name: Setup node and enable yarn caching
              uses: actions/setup-node@v4
              with:
                  node-version: 20
                  cache: "yarn"
                  cache-dependency-path: "web/yarn.lock"

            - name: Install dependencies
              run: yarn install

            - name: Build photos
              run: yarn build:photos

            - name: Publish photos
              uses: cloudflare/wrangler-action@v3
              env:
                NEXT_PUBLIC_ENTE_ENDPOINT: 'https://api.kanz.pictures'
                NEXT_PUBLIC_ENTE_ALBUMS_ENDPOINT: 'https://albums.kanz.pictures'
              with:
                  accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
                  apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
                  command: pages deploy --project-name=kanz --commit-dirty=true --branch=deploy web/apps/photos/out